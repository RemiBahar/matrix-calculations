doctype html
html(lang='en')
 head
   title #{title}
 body
   <nav>
   a(href="/") Home |
   a(href="/upload-matrix") Upload |
   if uploaded
    a(href="/deadline/calculation/add") Add |
    a(href="/deadline/calculation/multiply") Multiply

   </nav>
   
   h1 #{title}
   block content